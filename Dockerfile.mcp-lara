FROM node:20-slim

WORKDIR /app

RUN npm install -g pnpm

RUN git clone https://github.com/translated/lara-mcp.git . && \
    pnpm install && \
    pnpm run build

EXPOSE 8000

ENV TRANSPORT=http
ENV PORT=8000
ENV HOST=0.0.0.0

CMD ["node", "dist/index.js"]
