FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir "MCP-timeserver" mcp starlette uvicorn

EXPOSE 8000

CMD ["python", "-m", "mcp_timeserver", "--transport", "sse", "--port", "8000", "--host", "0.0.0.0"]
