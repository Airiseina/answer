FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir mcp_weather_server starlette uvicorn

EXPOSE 8080

CMD ["python", "-m", "mcp_weather_server", "--mode", "sse", "--host", "0.0.0.0", "--port", "8080"]
