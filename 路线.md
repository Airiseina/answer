AIM 核心开发路线图（修订版）

Phase 1: 关系链基建（地基）
目标：跑通用户体系与好友/群组关系，验证 Hertz + Kitex 的基础交互。
开发内容：
Hertz 处理用户登录/注册、JWT 鉴权。
好友关系 CRUD（添加、删除、分组、备注）。
群组管理 CRUD（创建、邀请、踢出、群公告等）。
存储：MySQL（用户表、好友关系表、群组表、群成员表）。
关键点：这一步完全不涉及 WebSocket，只跑通短连接 RESTful API。

Phase 2: 核心闭环——纯文本单聊 + 同步落库 + 云端漫游（核心骨架）
目标：跑通最基础的消息收发，坚决贯彻“先落库再推送”的原则，实现历史消息拉取。
开发内容：
定义协议：编写 Protobuf/Thrift IDL，定义基础消息结构（包含 client_seq, sender_id, receiver_id, content, timestamp）。
接入层：开发独立的 WebSocket 服务，完成握手、鉴权、连接管理。
逻辑层：开发 Kitex Chat Logic 服务。收到消息后，生成雪花 msg_id，直接写入 PostgreSQL。
在线推送：落库成功后，若接收方在线，通过 WS 推送给对方；不在线则不推。
漫游接口：Hertz 提供 /v1/chat/history 接口，客户端打开聊天框时，从 PostgreSQL 分页拉取历史（这就是云端漫游）。
存储：PostgreSQL（消息表 message_table，按时间分区），Redis（记录用户在线状态及所在网关节点）。

Phase 3: 丰富媒体消息 + 对象存储（扩展消息类型）
目标：支持图片、文件、语音等大文件消息。
开发内容：
Hertz 开发 /v1/upload 接口，处理文件上传。
引入 MinIO（本地自建对象存储，兼容 S3）。
修改 WS 消息体，content 字段统一改为 JSONB 格式，如：{"type": "image", "url": "minio_url", "size": 1024}。
存储：MinIO（存二进制文件），PostgreSQL（存 JSONB 元数据）。

Phase 4: 高频轻状态——在线、输入、已读（提升体验）
目标：利用 Redis 的高性能处理不需要强一致性的实时状态。
开发内容：
在线状态：WS 连接/断开时，更新 Redis 键值（带 TTL，心跳续期）。
输入状态：完全不落库，A 发送输入事件，WS 层查 Redis 发现 B 在线，直接透传。
已读回执：A 发送已读确认，服务端更新 PostgreSQL 中消息的 read_status，并在 Redis 中更新该会话的 max_read_seq（用于快速计算未读数）。
存储：Redis（状态缓存），PostgreSQL（状态持久化兜底）。

Phase 5: 消息控制——限时撤回与编辑（纠正误区）
目标：实现消息的修改与撤回，明确 Redis 只是辅助，PG 才是真相。
开发内容：
撤回：A 请求撤回 -> Logic 层用 Redis 的 TTL 快速判断是否在 2 分钟内 -> 是则 UPDATE PostgreSQL SET status='recalled' -> WS 通知 B 端刷新 UI。
编辑：类似撤回，UPDATE PostgreSQL SET content=new_content, is_edited=1 -> WS 通知对方。
存储：Redis（限时校验拦截），PostgreSQL（最终状态变更）。

Phase 6: 可靠性增强——离线推送与上线同步（防丢消息）
目标：保证弱网和离线场景下消息绝对不丢。
开发内容：
上线同步（拉模型）：客户端断线重连后，拿本地最大的 msg_seq 请求 Hertz，服务端从 PostgreSQL 拉取 seq > client_seq 的所有消息下推。
离线推送：发消息时判断对方不在线，触发 Hook，调用极光/FCM 等第三方 SDK 发送系统通知栏消息。
存储：PostgreSQL（消息源），第三方推送服务。

Phase 7: 终极挑战——多端同步（进阶门槛）
目标：同一账号多设备登录，消息和已读状态实时一致。
开发内容：
协议中增加 device_id 和 platform 标识。
Redis 中在线状态改为 Set 结构，记录该用户所有在线设备。
发消息/状态变更时，遍历 Redis 中的设备列表，向所有网关实例推送。
处理多端已读状态同步（一端已读，其他端未读数清零）。
存储：Redis（多端设备映射），PostgreSQL。

Phase 8: AI 灵魂注入（项目核心亮点）
目标：接入大模型，实现 Bot 对话、计费与 RAG 知识库。
开发内容：
Bot 路由：Chat Logic 识别 @Bot，异步调用 AI Service。
流式输出：AI Service 通过 SSE/WS Stream 将大模型生成的字逐一推回给 WS 接入层下发给客户端。
计费管理：MySQL 记录 API Key 余额和 Token 消耗流水。
RAG 知识库：实现文件上传 -> 文本提取 -> 向量化 -> 存入 PostgreSQL (pgvector) -> 相似度检索 -> 组装 Prompt。
存储：MySQL（计费与Key管理），PostgreSQL + pgvector（向量存储与检索），MinIO（知识库源文件）。

Phase 9: 分布式治理与可观测性（压轴基建）
目标：让系统具备生产级的抗风险能力与排障能力。
开发内容：
链路追踪：在 WS 握手时生成 trace_id，注入 Context，贯穿 Kitex RPC 和 MySQL/PG 请求，接入 Jaeger。
指标监控：在关键链路（消息收发、Bot 响应）埋点，接入 Prometheus，使用 Grafana 画大盘（在线数、吞吐量、Bot 延迟）。
服务治理：配置 Kitex 熔断、限流、超时重试策略。
容器化：编写 Dockerfile 和 Docker-Compose，一键拉起全部依赖（MySQL, PG, Redis, MinIO, Jaeger 等）。

这是我的首版文档，请你记住，然后再看下面这一篇修订版

修订后的 AIM 开发路线图（统一会话版）
Phase 1: 关系链与群组基建（调整）
目标：跑通用户体系与好友/群组关系。

变化：好友关系依然在 MySQL。但创建群组时，必须同步创建一条 conversation 记录，并在 conversation_member 中插入所有群成员。
单聊的隐式创建：单聊不需要显式“创建好友会话”。当 A 第一次给 B 发消息时，后端查 Redis 缓存，如果 A 和 B 没有单聊会话，则自动创建一个 type=1 的 conversation，并插入 A 和 B 两条成员记录。
Phase 2: 核心闭环——面向会话的消息收发（大变化）
目标：跑通基于 conversation_id 的消息流转。

开发内容：
协议变更：客户端发送消息时，不再传 receiver_id，而是传 conversation_id。（单聊时，客户端从会话列表获取；第一次发消息时，先调接口创建会话）。
逻辑层：收到消息后，以 conversation_id 为分区键写入 PostgreSQL。
在线推送：落库后，查询该 conversation_id 下的所有在线成员（查 Redis），通过 WS 推送给她们。
好处：发消息的 RPC 接口只有一个，不需要写 SendSingleMsg 和 SendGroupMsg。
Phase 3: 丰富媒体消息 + SeaweedFS（微调）
目标：支持图片、文件、语音。

变化：无实质变化。上传走 Hertz + SeaweedFS，返回 URL 后，将 URL 封装进消息的 JSONB content 中，发送给指定的 conversation_id。
Phase 4: 统一的未读数与已读回执（核心难点）
目标：基于 max_read_seq 实现精准的未读计算。

开发内容：
写扩散：一条消息落库后，该会话的未读数怎么加？不需要给每个人加！只需要在 Redis 里维护一个 conv:max_seq:{conversation_id} 递增即可。
计算未读：客户端拉取会话列表时，拿本地存储的 max_read_seq 与 Redis 里的 conv:max_seq 相减，就是未读数。
已读回执：A 标记已读时，更新 PostgreSQL 的 conversation_member 表中 A 的 max_read_seq，并同步到 Redis。多端同步时，其他端收到 max_read_seq 更新通知，重新计算未读数清零。
好处：单聊和群聊的未读数计算逻辑完全一致，不需要两套代码。
Phase 5: 消息控制——撤回与编辑（统一逻辑）
目标：限时撤回与编辑。

变化：撤回和编辑都是针对 message_id 的操作。校验权限时，只需查 conversation_member 中发送者的角色或是否为消息原作者即可。逻辑完全统一。
Phase 6: 云端漫游与上线同步（简化）
目标：断网重连后拉取缺失消息。

变化：客户端重连后，遍历本地所有的 conversation，拿着每个会话本地最大的 seq，向 Hertz 发起请求，拉取 seq > local_seq 的消息。这种基于会话维度的拉取，天然支持多端同步。
Phase 7: 群聊特有功能（附加层）
目标：禁言、群公告、转让群主。

变化：这些是 conversation_type = 2 时的特有逻辑。在 Kitex Logic 层做路由判断：如果是单聊，拒绝执行禁言操作；如果是群聊，校验 conversation_member 中的 role 权限后执行。
Phase 8: AI 灵魂注入（完美契合）
目标：接入大模型与 RAG。

变化：Bot 也是一个用户！将 Bot 作为一个特殊的 user_id 加入到 conversation_member 中。当会话中有人 @Bot 时，AI 服务接管。Bot 的回复也是往这个 conversation_id 里插入一条 sender_id = bot_id 的消息。AI 的上下文，就是该 conversation_id 下的最近 N 条消息，查询极其方便！
Phase 9: 分布式治理与可观测性
目标：上监控、上治理。

变化：无实质变化。